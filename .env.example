# Example environment variables for frontend
# Set this to the base URL of your API (auth_service or api_gateway)
# For local development with auth_service running on port 8000:
VITE_API_BASE_URL=http://127.0.0.1:8000

# If you run an API gateway on :9000, point here instead:
# VITE_API_BASE_URL=http://127.0.0.1:9000

# Optional: chunk upload threshold override (bytes)
# VITE_CHUNKED_THRESHOLD=10485760
